/*
Copyright The Kubernetes Authors.

Licensed under the Apache License, Version 2.0 (the "License");
you may not use this file except in compliance with the License.
You may obtain a copy of the License at

    http://www.apache.org/licenses/LICENSE-2.0

Unless required by applicable law or agreed to in writing, software
distributed under the License is distributed on an "AS IS" BASIS,
WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
See the License for the specific language governing permissions and
limitations under the License.
*/

// Code generated by lister-gen. DO NOT EDIT.

package v1

import (
	"github.com/kcp-dev/apimachinery/pkg/logicalcluster"
	"github.com/kcp-dev/client-gen/listerpoc/example"
	corev1 "k8s.io/api/core/v1"
	"k8s.io/apimachinery/pkg/api/errors"
	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
	"k8s.io/apimachinery/pkg/labels"
	"k8s.io/client-go/tools/cache"
)

// ConfigMapLister helps list ConfigMaps.
// All objects returned here must be treated as read-only.
type ConfigMapLister interface {
	// List lists all ConfigMaps in the indexer.
	// Objects returned here must be treated as read-only.
	List(selector labels.Selector) (ret []*corev1.ConfigMap, err error)

	// ConfigMaps returns an object that can list and get ConfigMaps.
	// ConfigMaps(cluster logicalcluster.LogicalCluster) ConfigMapClusterLister

	// Cluster returns an object that can list and get ConfigMaps from the given logical cluster.
	Cluster(cluster logicalcluster.LogicalCluster) ConfigMapClusterLister
	// Note(kcp): Workspace-capable Lister implementation doesn't support support expansions.
	// ConfigMapListerExpansion
}

// configMapLister implements the ConfigMapLister interface.
type configMapLister struct {
	indexer cache.Indexer
}

// NewConfigMapLister returns a new ConfigMapLister.
func NewConfigMapLister(indexer cache.Indexer) ConfigMapLister {
	return &configMapLister{indexer: indexer}
}

// List lists all ConfigMaps in the indexer.
func (s *configMapLister) List(selector labels.Selector) (ret []*corev1.ConfigMap, err error) {
	err = cache.ListAll(s.indexer, selector, func(m interface{}) {
		ret = append(ret, m.(*corev1.ConfigMap))
	})
	return ret, err
}

// Cluster returns an object that can list and get ConfigMaps.
func (s *configMapLister) Cluster(cluster logicalcluster.LogicalCluster) ConfigMapClusterLister {
	return &configMapClusterLister{indexer: s.indexer, cluster: cluster}
}

// ConfigMapLister helps list ConfigMaps.
// All objects returned here must be treated as read-only.
type ConfigMapClusterLister interface {
	// List lists all ConfigMaps in the indexer.
	// Objects returned here must be treated as read-only.
	List(selector labels.Selector) (ret []*corev1.ConfigMap, err error)
	// ConfigMaps returns an object that can list and get ConfigMaps.
	ConfigMaps(namespace string) ConfigMapNamespaceLister
	// Note(kcp): Workspace-capable Lister implementation doesn't support support expansions.
	// ConfigMapListerExpansion
}

// configMapClusterLister implements the ConfigMapLister interface.
type configMapClusterLister struct {
	indexer cache.Indexer
	cluster logicalcluster.LogicalCluster
}

// List lists all ConfigMaps in the indexer.
func (c *configMapClusterLister) List(selector labels.Selector) (ret []*corev1.ConfigMap, err error) {
	list, err := c.indexer.ByIndex(example.ClusterIndexName, c.cluster.String())
	if err != nil {
		return nil, err
	}

	if selector == nil {
		selector = labels.Everything()
	}
	for i := range list {
		cm := list[i].(*corev1.ConfigMap)
		if selector.Matches(labels.Set(cm.GetLabels())) {
			ret = append(ret, cm)
		}
	}

	return ret, err
}

// ConfigMaps returns an object that can list and get ConfigMaps.
func (c *configMapClusterLister) ConfigMaps(namespace string) ConfigMapNamespaceLister {
	return configMapNamespaceLister{indexer: c.indexer, cluster: c.cluster, namespace: namespace}
}

// ConfigMapNamespaceLister helps list and get ConfigMaps.
// All objects returned here must be treated as read-only.
type ConfigMapNamespaceLister interface {
	// List lists all ConfigMaps in the indexer for a given namespace.
	// Objects returned here must be treated as read-only.
	List(selector labels.Selector) (ret []*corev1.ConfigMap, err error)
	// Get retrieves the ConfigMap from the indexer for a given namespace and name.
	// Objects returned here must be treated as read-only.
	Get(name string) (*corev1.ConfigMap, error)
	// Note(kcp): Workspace-capable Lister implementation doesn't support support expansions.
	// ConfigMapNamespaceListerExpansion
}

// configMapNamespaceLister implements the ConfigMapNamespaceLister
// interface.
type configMapNamespaceLister struct {
	indexer   cache.Indexer
	cluster   logicalcluster.LogicalCluster
	namespace string
}

// List lists all ConfigMaps in the indexer for a given namespace.
func (c configMapNamespaceLister) List(selector labels.Selector) (ret []*corev1.ConfigMap, err error) {
	list, err := c.indexer.Index(example.ClusterAndNamespaceIndexName, &metav1.ObjectMeta{
		ZZZ_DeprecatedClusterName: c.cluster.String(),
		Namespace:                 c.namespace,
	})
	if err != nil {
		return nil, err
	}

	if selector == nil {
		selector = labels.Everything()
	}
	for i := range list {
		cm := list[i].(*corev1.ConfigMap)
		if selector.Matches(labels.Set(cm.GetLabels())) {
			ret = append(ret, cm)
		}
	}

	return ret, err
}

// Get retrieves the ConfigMap from the indexer for a given namespace and name.
func (c configMapNamespaceLister) Get(name string) (*corev1.ConfigMap, error) {
	meta := &metav1.ObjectMeta{
		ZZZ_DeprecatedClusterName: c.cluster.String(),
		Namespace:                 c.namespace,
		Name:                      name,
	}
	obj, exists, err := c.indexer.Get(meta)
	if err != nil {
		return nil, err
	}
	if !exists {
		return nil, errors.NewNotFound(corev1.Resource("configmaps"), name)
	}
	return obj.(*corev1.ConfigMap), nil
}
