//go:build !ignore_autogenerated
// +build !ignore_autogenerated

/*
Copyright The KCP Authors.

Licensed under the Apache License, Version 2.0 (the "License");
you may not use this file except in compliance with the License.
You may obtain a copy of the License at

    http://www.apache.org/licenses/LICENSE-2.0

Unless required by applicable law or agreed to in writing, software
distributed under the License is distributed on an "AS IS" BASIS,
WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
See the License for the specific language governing permissions and
limitations under the License.
*/

// Code generated by kcp code-generator. DO NOT EDIT.

package v1

import (
	kcpcache "github.com/kcp-dev/apimachinery/v2/pkg/cache"
	"github.com/kcp-dev/logicalcluster/v3"

	"k8s.io/apimachinery/pkg/api/errors"
	"k8s.io/apimachinery/pkg/labels"
	"k8s.io/client-go/tools/cache"

	corev1 "acme.corp/pkg/apis/core/v1"
)

// ConfigMapClusterLister can list ConfigMaps across all workspaces, or scope down to a ConfigMapLister for one workspace.
// All objects returned here must be treated as read-only.
type ConfigMapClusterLister interface {
	// List lists all ConfigMaps in the indexer.
	// Objects returned here must be treated as read-only.
	List(selector labels.Selector) (ret []*corev1.ConfigMap, err error)
	// Cluster returns a lister that can list and get ConfigMaps in one workspace.
	Cluster(clusterName logicalcluster.Name) ConfigMapLister
	ConfigMapClusterListerExpansion
}

type configMapClusterLister struct {
	indexer cache.Indexer
}

// NewConfigMapClusterLister returns a new ConfigMapClusterLister.
// We assume that the indexer:
// - is fed by a cross-workspace LIST+WATCH
// - uses kcpcache.MetaClusterNamespaceKeyFunc as the key function
// - has the kcpcache.ClusterIndex as an index
// - has the kcpcache.ClusterAndNamespaceIndex as an index
func NewConfigMapClusterLister(indexer cache.Indexer) *configMapClusterLister {
	return &configMapClusterLister{indexer: indexer}
}

// List lists all ConfigMaps in the indexer across all workspaces.
func (s *configMapClusterLister) List(selector labels.Selector) (ret []*corev1.ConfigMap, err error) {
	err = cache.ListAll(s.indexer, selector, func(m interface{}) {
		ret = append(ret, m.(*corev1.ConfigMap))
	})
	return ret, err
}

// Cluster scopes the lister to one workspace, allowing users to list and get ConfigMaps.
func (s *configMapClusterLister) Cluster(clusterName logicalcluster.Name) ConfigMapLister {
	return &configMapLister{indexer: s.indexer, clusterName: clusterName}
}

// ConfigMapLister can list ConfigMaps across all namespaces, or scope down to a ConfigMapNamespaceLister for one namespace.
// All objects returned here must be treated as read-only.
type ConfigMapLister interface {
	// List lists all ConfigMaps in the workspace.
	// Objects returned here must be treated as read-only.
	List(selector labels.Selector) (ret []*corev1.ConfigMap, err error)
	// ConfigMaps returns a lister that can list and get ConfigMaps in one workspace and namespace.
	ConfigMaps(namespace string) ConfigMapNamespaceLister
	ConfigMapListerExpansion
}

// configMapLister can list all ConfigMaps inside a workspace or scope down to a ConfigMapLister for one namespace.
type configMapLister struct {
	indexer     cache.Indexer
	clusterName logicalcluster.Name
}

// List lists all ConfigMaps in the indexer for a workspace.
func (s *configMapLister) List(selector labels.Selector) (ret []*corev1.ConfigMap, err error) {
	err = kcpcache.ListAllByCluster(s.indexer, s.clusterName, selector, func(i interface{}) {
		ret = append(ret, i.(*corev1.ConfigMap))
	})
	return ret, err
}

// ConfigMaps returns an object that can list and get ConfigMaps in one namespace.
func (s *configMapLister) ConfigMaps(namespace string) ConfigMapNamespaceLister {
	return &configMapNamespaceLister{indexer: s.indexer, clusterName: s.clusterName, namespace: namespace}
}

// configMapNamespaceLister helps list and get ConfigMaps.
// All objects returned here must be treated as read-only.
type ConfigMapNamespaceLister interface {
	// List lists all ConfigMaps in the workspace and namespace.
	// Objects returned here must be treated as read-only.
	List(selector labels.Selector) (ret []*corev1.ConfigMap, err error)
	// Get retrieves the ConfigMap from the indexer for a given workspace, namespace and name.
	// Objects returned here must be treated as read-only.
	Get(name string) (*corev1.ConfigMap, error)
	ConfigMapNamespaceListerExpansion
}

// configMapNamespaceLister helps list and get ConfigMaps.
// All objects returned here must be treated as read-only.
type configMapNamespaceLister struct {
	indexer     cache.Indexer
	clusterName logicalcluster.Name
	namespace   string
}

// List lists all ConfigMaps in the indexer for a given workspace and namespace.
func (s *configMapNamespaceLister) List(selector labels.Selector) (ret []*corev1.ConfigMap, err error) {
	err = kcpcache.ListAllByClusterAndNamespace(s.indexer, s.clusterName, s.namespace, selector, func(i interface{}) {
		ret = append(ret, i.(*corev1.ConfigMap))
	})
	return ret, err
}

// Get retrieves the ConfigMap from the indexer for a given workspace, namespace and name.
func (s *configMapNamespaceLister) Get(name string) (*corev1.ConfigMap, error) {
	key := kcpcache.ToClusterAwareKey(s.clusterName.String(), s.namespace, name)
	obj, exists, err := s.indexer.GetByKey(key)
	if err != nil {
		return nil, err
	}
	if !exists {
		return nil, errors.NewNotFound(corev1.Resource("configmaps"), name)
	}
	return obj.(*corev1.ConfigMap), nil
}

// NewConfigMapLister returns a new ConfigMapLister.
// We assume that the indexer:
// - is fed by a workspace-scoped LIST+WATCH
// - uses cache.MetaNamespaceKeyFunc as the key function
// - has the cache.NamespaceIndex as an index
func NewConfigMapLister(indexer cache.Indexer) *configMapScopedLister {
	return &configMapScopedLister{indexer: indexer}
}

// configMapScopedLister can list all ConfigMaps inside a workspace or scope down to a ConfigMapLister for one namespace.
type configMapScopedLister struct {
	indexer cache.Indexer
}

// List lists all ConfigMaps in the indexer for a workspace.
func (s *configMapScopedLister) List(selector labels.Selector) (ret []*corev1.ConfigMap, err error) {
	err = cache.ListAll(s.indexer, selector, func(i interface{}) {
		ret = append(ret, i.(*corev1.ConfigMap))
	})
	return ret, err
}

// ConfigMaps returns an object that can list and get ConfigMaps in one namespace.
func (s *configMapScopedLister) ConfigMaps(namespace string) ConfigMapNamespaceLister {
	return &configMapScopedNamespaceLister{indexer: s.indexer, namespace: namespace}
}

// configMapScopedNamespaceLister helps list and get ConfigMaps.
type configMapScopedNamespaceLister struct {
	indexer   cache.Indexer
	namespace string
}

// List lists all ConfigMaps in the indexer for a given workspace and namespace.
func (s *configMapScopedNamespaceLister) List(selector labels.Selector) (ret []*corev1.ConfigMap, err error) {
	err = cache.ListAllByNamespace(s.indexer, s.namespace, selector, func(i interface{}) {
		ret = append(ret, i.(*corev1.ConfigMap))
	})
	return ret, err
}

// Get retrieves the ConfigMap from the indexer for a given workspace, namespace and name.
func (s *configMapScopedNamespaceLister) Get(name string) (*corev1.ConfigMap, error) {
	key := s.namespace + "/" + name
	obj, exists, err := s.indexer.GetByKey(key)
	if err != nil {
		return nil, err
	}
	if !exists {
		return nil, errors.NewNotFound(corev1.Resource("configmaps"), name)
	}
	return obj.(*corev1.ConfigMap), nil
}
